<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="resume.core.mapper.UserMapper">

    <select id="findByUserKey" parameterType="string" resultType="resume.core.dto.UserDto">
        /* UserMapper.findByUserKey */
        SELECT
            user_id
            , user_key
            , name
            , created_at
            , updated_at
        FROM users
        WHERE user_key = #{userKey}
    </select>

    <insert id="insertUser" parameterType="resume.core.dto.UserDto" useGeneratedKeys="true" keyProperty="userId">
        /* UserMapper.insertUser */
        INSERT INTO users (
            user_key,
            name
        ) VALUES (
            #{userKey},
            #{name}
        )
    </insert>
</mapper>